Data Agent Prompt Template

Think:
- Load CSV, ensure types, compute aggregates.

Analyze:
- Provide campaign-level and month-level summaries:
  - total_spend, impressions, clicks, avg_ctr, avg_roas, unique_creatives

Conclude:
- Return JSON with summary and sampling instructions.